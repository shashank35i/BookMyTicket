# Screenshots and Demo Capture

No verified runtime screenshots or demo GIF are checked in yet. Product artwork in `app/src/main/res/drawable` must not be labeled as an application screenshot.

Capture from a seeded non-production Firebase project using fictional data:

1. Phone OTP and role selection.
2. Tourist dashboard, ticket purchase, active QR, and history.
3. Place-admin dashboard and QR validation.
4. Parking-admin dashboard and vehicle scan.
5. Notification and payment-success states.

Use a consistent emulator profile, hide system notifications, redact phone numbers and IDs, and record the app version/commit. Store optimized images here as `tourist-dashboard.webp`, `ticket-qr.webp`, `place-admin.webp`, and `parking-admin.webp`. Store a short optimized walkthrough as `demo.gif` or link a release video.

After capture, replace the status text in the root README with a small table of these artifacts.
